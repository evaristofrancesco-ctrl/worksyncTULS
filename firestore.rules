
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permettiamo tutto agli utenti autenticati per risolvere problemi di accesso immediato.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
